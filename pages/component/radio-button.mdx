import { SingleGroup } from '@components/templates/radioButton/SingleGroup'
import { ExampleContainer } from '@components/atoms/ExampleContainer'

# Animated Radio Button

A fun radio button component with character.

Built with accessibility in mind, following the [W3C Guidelines](https://www.w3.org/tr/2017/wd-wai-aria-practices-1.1-20170628/examples/radio/radio-1/radio-1.html).

### Single Group

Using its medium variant, starting with the first option selected at start, using the `initialActive` prop.

<ExampleContainer>
  <SingleGroup />
</ExampleContainer>
